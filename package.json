{
  "name": "ai-predictive-stress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix base",
    "build": "npm run build --prefix base",
    "lint": "npm run lint --prefix base",
    "typecheck": "npm run typecheck --prefix base",
    "preview": "npm run preview --prefix base"
  }
}
